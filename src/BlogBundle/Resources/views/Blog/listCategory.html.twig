<div class="list-group">
    {% for category in categories %}
        <a href="{{ path('blog_post_by_category', { 'id': category.id }) }}" class="list-group-item{% if currId == category.id %} active{% endif %}">{{ category.name }}</a>
    {% endfor %}
</div>